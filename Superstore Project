# write a SQL query that orders the items by price

SELECT *
FROM superstore
ORDER BY price
;

# write your own SQL query that will show you a statistic about the item prices, like sum, avg, minimum, maximum, or count

SELECT AVG(price)
FROM superstore
;

# write your own SQL query that will show you a statistic about the item price for items in the category "Kitchen Supplies"

SELECT MAX(price)
FROM superstore
WHERE category = 'Kitchen Supplies'
;

# come up with your own query
# write your own SQL query that will show you what item has the highest average rating in the electronics category

SELECT item_name
, average_rating
FROM superstore
WHERE category = 'electronics'
ORDER BY average_rating DESC
;
